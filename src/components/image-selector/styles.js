import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    textPic: {
        paddingTop: 150,
        color: 'white'
    },
    image: {
        borderRadius: 100,
        marginTop: 80,
        marginBottom: 10,
        width: 200,
        height: 200,
      },
});